import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth } from "../../../../../../../../BaseClient.js";
import * as core from "../../../../../../../../core/index.js";
import * as Deepgram from "../../../../../../../index.js";
export declare namespace MediaClient {
    type Options = BaseClientOptions;
    interface RequestOptions extends BaseRequestOptions {
    }
}
export declare class MediaClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<MediaClient.Options>;
    constructor(options?: MediaClient.Options);
    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     *
     * @param {Deepgram.listen.v1.ListenV1RequestUrl} request
     * @param {MediaClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Deepgram.BadRequestError}
     *
     * @example
     *     await client.listen.v1.media.transcribeUrl({
     *         callback: "callback",
     *         callback_method: "POST",
     *         extra: "extra",
     *         sentiment: true,
     *         summarize: "v2",
     *         tag: "tag",
     *         topics: true,
     *         custom_topic: "custom_topic",
     *         custom_topic_mode: "extended",
     *         intents: true,
     *         custom_intent: "custom_intent",
     *         custom_intent_mode: "extended",
     *         detect_entities: true,
     *         detect_language: true,
     *         diarize: true,
     *         dictation: true,
     *         encoding: "linear16",
     *         filler_words: true,
     *         keywords: "keywords",
     *         language: "language",
     *         measurements: true,
     *         model: "nova-3",
     *         multichannel: true,
     *         numerals: true,
     *         paragraphs: true,
     *         profanity_filter: true,
     *         punctuate: true,
     *         redact: "redact",
     *         replace: "replace",
     *         search: "search",
     *         smart_format: true,
     *         utterances: true,
     *         utt_split: 1.1,
     *         version: "latest",
     *         mip_opt_out: true,
     *         url: "https://dpgr.am/spacewalk.wav"
     *     })
     */
    transcribeUrl(request: Deepgram.listen.v1.ListenV1RequestUrl, requestOptions?: MediaClient.RequestOptions): core.HttpResponsePromise<Deepgram.listen.v1.MediaTranscribeResponse>;
    private __transcribeUrl;
    /**
     * Transcribe audio and video using Deepgram's speech-to-text REST API
     *
     * @param {core.file.Uploadable} uploadable
     * @param {Deepgram.listen.v1.MediaTranscribeRequestOctetStream} request
     * @param {MediaClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Deepgram.BadRequestError}
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.listen.v1.media.transcribeFile(createReadStream("path/to/file"), {})
     */
    transcribeFile(uploadable: core.file.Uploadable, request: Deepgram.listen.v1.MediaTranscribeRequestOctetStream, requestOptions?: MediaClient.RequestOptions): core.HttpResponsePromise<Deepgram.listen.v1.MediaTranscribeResponse>;
    private __transcribeFile;
}
